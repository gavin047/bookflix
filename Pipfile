[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.1.0"
flask-sqlalchemy = ">=2.5.1"
flask-migrate = ">=3.1.0"
flask-cors = ">=3.0.10"
flask-bcrypt = ">=0.7.1"
flask-jwt-extended = ">=4.4.0"
werkzeug = ">=2.0"

[dev-packages]

[requires]
python_version = "3.8"
